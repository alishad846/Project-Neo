import { defineConfig } from "wxt";

export default defineConfig({
  manifest: {
    name: "Project Neo",
    short_name: "Neo",
    description:
      "AI-powered browser extension for automating seller product catalogues.",
    version: "1.0.0",
  },
});